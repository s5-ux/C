#include<stdio.h>
void main()
{
    int a;
    printf("Enter a num");
    scanf("%d",&a);
    int sum=0;;
    while(a>=1)
    {
        sum=sum+a%10;
        a=a/10;
    }
    printf("The sum of digits is %d",sum);
}
