#include <stdio.h>

int main() {
    int a, sum = 0; 
    printf("Enter one number: ");
    scanf("%d", &a);
    
    for (int i = 1; i < a; i++) {
        sum = sum + i; 
        printf("\nHi %d", i);
    }
    
    printf("\nThe sum is %d\n", sum); 
    return 0; 
}
